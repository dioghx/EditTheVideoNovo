<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.video.startup.editthevideodio">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MostarProfissional"/>
        <activity android:name=".activity.EscolherCadastroActivity"/>
        <activity android:name=".activity.CriarUsuarioActivity"/>
        <activity android:name=".activity.CriarEmpresaActivity"/>
        <activity android:name=".activity.CriarFreeActivity"/>
        <activity android:name=".activity.TelaLoginActivity"/>

    </application>

</manifest>